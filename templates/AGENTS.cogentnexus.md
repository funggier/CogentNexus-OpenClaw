## CogentNexus - Mandatory Cognitive Runtime

Use CogentNexus for every user request and reasoning task in this workspace.

1. Load and apply the `cogentnexus` skill before reasoning or responding.
2. Follow its Kernel for every request.
3. Load only the internal modules routed by `cogentnexus/SKILL.md` when the task needs them.
4. Choose the lightest reliable lane and verify evidence before claiming completion.

For simple conversation, apply the framework internally and answer concisely. Do not expose private chain-of-thought or add unnecessary process narration. User intent, authorization, safety rules, and higher-priority instructions always take precedence.
