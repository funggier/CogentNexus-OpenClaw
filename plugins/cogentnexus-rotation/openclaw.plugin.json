{
  "id": "cogentnexus-rotation",
  "name": "CogentNexus Rotation Controller",
  "description": "Validate a durable CogentNexus ROTATE handoff and start one idempotently identified TaskFlow worker.",
  "version": "0.2.1",
  "configSchema": {
    "type": "object",
    "properties": {
      "cogentRoot": {
        "type": "string"
      },
      "pythonCommand": {
        "type": "string"
      },
      "agentId": {
        "type": "string"
      },
      "model": {
        "type": "string"
      },
      "timeoutSeconds": {
        "type": "integer",
        "minimum": 60,
        "maximum": 86400
      },
      "autoResume": {
        "type": "boolean",
        "description": "Schedule one durable continuation turn after a resumable interruption."
      },
      "autoResumeDelayMs": {
        "type": "integer",
        "minimum": 1000,
        "maximum": 60000
      },
      "autoRotate": {
        "type": "boolean",
        "description": "Opt in to a clean TaskFlow/Codex worker for ROTATE handoffs. Disabled by default."
      },
      "workspaceDir": {
        "type": "string",
        "description": "Workspace containing .cogent/workflows completion outboxes."
      },
      "autoWorkflowCompletion": {
        "type": "boolean",
        "description": "Automatically wake the bound owner when a workflow reaches a terminal state."
      },
      "completionPollMs": {
        "type": "integer",
        "minimum": 1000,
        "maximum": 300000
      },
      "enforcedMode": {
        "type": "boolean",
        "description": "Block conversational attempts to create unbound workflows or bind owner identity manually."
      },
      "preInferenceAdmission": {
        "type": "boolean",
        "description": "Route obvious durable owner requests before the selected conversational model receives the prompt."
      },
      "admissionMinimumScore": {
        "type": "integer",
        "minimum": 3,
        "maximum": 20
      },
      "durableWorkerModel": {
        "type": "string",
        "description": "Ollama model used by automatically compiled bounded workflow components."
      }
    },
    "additionalProperties": false
  },
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": [
      "cogent_rotation",
      "cogent_workflow_start"
    ]
  },
  "toolMetadata": {
    "cogent_rotation": {
      "optional": true
    },
    "cogent_workflow_start": {
      "optional": true
    }
  }
}
