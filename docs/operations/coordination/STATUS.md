# Coordination Channel Status

**State:** `READY_FOR_HERMES`
**Updated:** 2026-08-27 ICT
**Transport:** GitHub repository history
**Human authority:** operator authorized continuation through definitive live repair and final authenticated fresh-session semantic acceptance
**Execution trigger:** manual Hermes continuation; scheduled execution remains disabled

## Accepted baseline

Accepted source/live lineage through Tasks 078/079/080, 082, 084/085/086, 089, 090 and 091 remains in force.

Task 090 live recovery remains the current installed baseline before Task 096: coherent MANAGED deployment, one canonical plugin generation, healthy Gateway/Ollama/SQLite and previously proven no-flash natural ticks.

Task 091 authenticated Dashboard/WebChat owner surface remains accepted without secret disclosure.

Task 092 remains retired semantic evidence: its first fresh session, Ticket-before-provider ordering, one Ollama inference and one visible nonce were proven, but delivery failed closed because durable final-payload staging was absent. Do not rewrite or reuse those records.

## Preserved source repairs

Task 093 Dashboard staging repair remains preserved:

`a924157ecdedef1d4f166d5762529b0d59536fc9`

Task 094 complete installable-payload v2 fingerprint remains preserved.

Task 095 implementation:

`32212a4331e1f32b5a130bd30d271d4cbc56f6c1`

Task 095 independent disposition:

`ACCEPT_WINDOWS_REPARSE_POINT_PAYLOAD_ATTESTATION_REPAIRED`

The exact released deploy source for the next live task is therefore:

`32212a4331e1f32b5a130bd30d271d4cbc56f6c1`

## Active Task 096

[`tasks/CNX-20260827-096-live-install-repaired-staging-and-restore-parity.md`](tasks/CNX-20260827-096-live-install-repaired-staging-and-restore-parity.md)

Execution mode:

`LIVE_ONE_SHOT_SUPPORTED_INSTALL_TASK093_095_SOURCE`

Authorization:

`TASK095_ACCEPTED_ONE_SHOT_LIVE_INSTALL_AUTHORIZED`

Exactly one supported normal install-over is authorized. No retry/manual repair is authorized.

Pre-mutation the real production v2 classifier/action boundary must prove the currently installed pre-Task093 plugin is non-exact relative to the candidate:

- `upgrade`;
- `pendingRollover=false`;
- `pluginAlreadyExact=false`;
- `installPlugin=true`;
- `rolloverPlugin=true`.

The supported installer must actually execute package installation and ownership-safe rollover, not skip as already exact.

## Required live outcome

For PASS Task 096 must converge to:

- exactly one canonical candidate-exact CogentNexus plugin generation;
- ownership manifest bound to that surviving generation;
- MANAGED controller/startup/Supervisor/AGENTS/source parity restored;
- Gateway and SQLite healthy;
- accepted Ollama inventory unchanged with zero direct model invocation;
- Task-092 retired semantic evidence unchanged;
- zero new Task-096 semantic/provider activity;
- at least five natural PT1M supervisor ticks with `NO_FLASH_MULTI_TICK_REPROVEN`;
- authenticated Dashboard owner/control readiness with no send.

## Hard fence

Task 096 sends zero semantic messages. No `chat.send`, `chat.inject`, `openclaw agent`, `sessions_send`, channel send, semantic nonce or direct Ollama/provider call is allowed.

No second installer attempt, manual plugin lifecycle command, manual generation cleanup, ownership/controller repair, reset/uninstall, SQLite mutation, Task-092 rewrite, provider/model/timeout change, reboot, merge/tag/release or force push is authorized.

## Successor logic

Only independent acceptance of:

`PASS_REPAIRED_STAGING_LIVE_INSTALLED_PARITY_READY`

may authorize one new final authenticated fresh-session semantic attempt.

That final task must use a new nonce exactly once through the authenticated Dashboard/WebChat owner surface, prove a genuinely new session after first send, prove durable Ticket acceptance/routing before Ollama, prove exact final payload staging before native delivery, settle delivery to `completed`, and then prove another New Session can be entered without stale/unknown-parent failure or additional semantic/provider effects.