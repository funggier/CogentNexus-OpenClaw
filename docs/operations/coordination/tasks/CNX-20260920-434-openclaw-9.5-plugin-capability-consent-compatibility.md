# CNX-20260920-434 — OpenClaw 9.5 Plugin Capability Consent Compatibility

Status: `COMPLETE`

Parent: `CNX-20260920-433`

## Trigger

The supported install-over from exact candidate:

`c77f393e0d46ff86731cb47e0fd9ffad98499fe3`

successfully crossed the CNX-433 rollover preparation boundary:

- `plugin-rollover-prepare` start: `2026-09-19T21:47:11.1614638Z`
- complete: `2026-09-19T21:47:12.2037552Z`
- elapsed: `1042 ms`
- exit code: `0`

The installer then stopped at:

`plugin-install-local-package`

with OpenClaw 2026.9.5 reporting:

`Plugin "cogentnexus-openclaw" requires capability consent.`

OpenClaw instructed the installer to rerun the same plugin install/update command with:

`--accept-capabilities`

No plugin replacement was committed.

## OpenClaw 9.5 CLI contract

Live CLI:

- OpenClaw: `2026.9.5 (ec9c1a1)`
- command: `openclaw plugins install --help`

documents:

`--accept-capabilities`

as:

`Accept the plugin's declared capabilities (default: false)`

The existing CNX installer already:

1. builds the plugin from the exact repository candidate;
2. validates the plugin;
3. bootstraps/verifies Ticket DB compatibility;
4. packs the local archive;
5. calculates the expected replacement fingerprint;
6. prepares the ownership-safe rollover backup and exact attestation;
7. invokes `openclaw plugins install` only on that same local package archive.

Therefore capability consent is bounded to the already validated, locally generated, source-attested plugin candidate. It does not accept arbitrary marketplace or external plugin capabilities.

## Required semantics

1. The supported installer explicitly accepts the capabilities declared by the exact local CNX package it is installing.
2. Existing `--force` behavior remains unchanged.
3. Source/package/fingerprint/rollover attestation remains unchanged.
4. The consent flag is present in both Windows and POSIX installers.
5. No global OpenClaw security policy bypass is added.
6. No dangerous install-policy override is added.
7. Failure/block semantics outside capability consent remain unchanged.

## TDD RED

Added:

`tests/test_openclaw95_plugin_capability_consent.py`

Initial result:

- Windows installer assertion: FAIL
- POSIX installer assertion: FAIL
- total: `2 failed`

because neither local-package install command included `--accept-capabilities`.

## Production repair

Changed:

- `scripts/install.ps1`
- `scripts/install.sh`

Windows command:

`openclaw plugins install $packagePath --force --accept-capabilities`

POSIX command:

`openclaw plugins install "npm-pack:$PLUGIN_DIR/$PACKAGE_FILE" --force --accept-capabilities`

No other install arguments were changed.

## GREEN evidence

Focused capability consent tests:

- `2/2 PASS`

Affected installer contract regression:

- `42 passed`
- `0 failed`

Covered:

- capability consent contract;
- fresh transaction failure coverage;
- installer observability contract;
- namespace install contract;
- npm-pack installer boundary.

## Live qualification plan

1. syntax-check PowerShell and POSIX installers;
2. run `git diff --check`;
3. freeze exact CNX-434 implementation commit;
4. verify PASSTHROUGH/Gateway/plugin baseline;
5. retry one supported install-over from the frozen candidate;
6. require:
   - rollover prepare exit `0`;
   - plugin local-package install exit `0`;
   - rollover finalize exit `0`;
   - complete installer exit `0`;
7. qualify installed plugin/runtime before final Discord acceptance.

## Final classification target

`OPENCLAW95_PLUGIN_CAPABILITY_CONSENT_GREEN`

## Live qualification result

The exact candidate `076d76d5465798753eda5146cdaa9cf7da72b5ff` executed:

`openclaw plugins install <local-package> --force --accept-capabilities`

Live result:

- `plugin-rollover-prepare`: exit `0`;
- `plugin-install-local-package`: exit `0`, elapsed `89630ms`;
- OpenClaw reported `Installed plugin: cogentnexus-openclaw` and `Applied in Gateway generation 3`;
- `plugin-disable-post-install`: exit `0`;
- `plugin-rollover-finalize`: exit `0`;
- `owned-runtime-ensure`: exit `0`.

The overall installer later failed at a distinct post-install Host-enable Gateway verification boundary. That later blocker does not invalidate capability-consent qualification.

Classification: `OPENCLAW95_PLUGIN_CAPABILITY_CONSENT_GREEN`
