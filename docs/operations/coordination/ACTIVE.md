# Active Coordination Task

Status: `READY_FOR_HERMES`
Execution mode: `TASK221_EXACT_FIRST_CHECKOUT_CONTROL_ADJUDICATION`
Current disposition: `TASK220_PARTIAL_ACCEPTED__STATIC_DIVERGENCE_PRE_NPM_PROVEN__EXACT_FIRST_CHECKOUT_REQUIRED`
Task ID: `CNX-20260901-221`
Parent task: `CNX-20260901-220`
Repair parent: `CNX-20260831-198`
Parent umbrella: `CNX-20260831-188`
Updated: 2026-09-01 ICT
Executor: Hermes / authenticated Windows repository operator
Coordinator / final reviewer: ChatGPT

## Published authority

Public `v0.9.3` remains immutable at:

`26ce64a624255278a3a0266ad38746e0e6ed2e31`

No Release/tag/asset mutation is authorized.

## Task-219 generated-output repair boundary

The genuine real-boundary RED and bounded generated-`dist` repair remain accepted evidence:

- RED: 188 LF-build files vs 188 CRLF-build files, 43 generated byte differences;
- bounded GREEN lineage: `9af329b4de7c02fda35b467d84e76bb0f0bb0944`;
- post-repair `dist` differences: 0;
- fingerprint algorithm unchanged.

The remaining package mismatch is static checkout materialization, not generated `dist`.

## Task-220 reviewed result

Report:

`reports/CNX-20260901-220-task219-static-payload-checkout-boundary-adjudication.md`

Review:

`reviews/CNX-20260901-220-task219-static-payload-checkout-boundary-adjudication-review.md`

Accepted disposition:

`ACCEPT_PARTIAL__STATIC_DIVERGENCE_PRE_NPM_PROVEN__EXACT_FIRST_CHECKOUT_CONTROL_REQUIRED`

Accepted facts:

- exact `4e31dbd79cd4c0a7eb161888c14221f0ae03bcc0` static Git objects are LF-only;
- Task-220 measured CRLF in `README.md`, `openclaw.plugin.json`, and `scripts/bootstrap-ticket-db.mjs` before npm/build;
- npm/build/plugin validation preserved those bytes and are not the first divergence;
- explicit `core.autocrlf=false` produced LF-only bytes in the Task-220 control;
- `-text` is not an accepted package-identity repair;
- Task-220 primary checkout was two-stage: clone current branch HEAD, then detach to `4e31dbd...`;
- `4e31dbd... -> b081d55c4ffa5fcb03931dc320d39bdcf92a6cf5` changes only `.gitattributes`, not the static payload blobs;
- therefore direct exact-commit checkout behavior is not yet isolated from attribute-state/working-tree carry-over.

## Active Task 221

Hermes must execute:

`tasks/CNX-20260901-221-task220-exact-first-checkout-control-adjudication.md`

Task 221 is diagnostic only. It must make exact `4e31dbd...` the **first-ever working-tree materialization** in independent controls using no-checkout/init+fetch topology, then compare:

- inherited/default Git config;
- explicit local `core.autocrlf=true`;
- explicit local `core.autocrlf=false`;
- Task-220 two-stage checkout topology;
- safe forced rematerialization if needed.

For every control record object/worktree hashes, newline counts, `git check-attr`, `git ls-files --eol`, status, and config origins.

No product fix is authorized until this distinction is closed.

## Runtime / Discord boundary

Task 221 authorizes `0 Discord Sends`.

No installer/install-over, lifecycle action, live plugin/config mutation, Gateway restart, live SQLite/ownership/transaction mutation, provider/model substitution, product/source/test/workflow edit, Release/tag mutation, force push, or unrelated process termination is authorized.
