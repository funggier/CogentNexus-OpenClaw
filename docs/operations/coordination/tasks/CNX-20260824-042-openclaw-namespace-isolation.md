# CNX-20260824-042 — Isolate the CogentNexus-OpenClaw Namespace

Status: READY  
Owner: ChatGPT  
Executor: Codex  
Execution mode: AUTO  
Predecessor: CNX-20260824-041 (reviewed BLOCKED; bounded non-recurrence accepted)

## Human direction

The operator chose to resume core project work rather than repeat Procmon observation and requested a product-wide namespace correction:

> ผมอยากให้มีการปรับเพื่อให้เป็น CogentNexus-OpenClaw ทั้งหมดครับ เช่น cnx.cmd เปลี่ยนชื่อเป็น cnxclaw.cmd ชื่อ skill จาก cogentnexus เปลี่ยนเป็น CogentNexus-OpenClaw และปรับชื่อของส่วนต่างๆ โดยไม่เรียก cogentnexus เฉยๆ ... เมื่อมีการติดตั้ง CogentNexus-HermesAgent ในเครื่องเดียวกัน จะได้ไม่ชนกัน

Task 041 artifacts must remain retained. Do not repeat capture or restoration.

## Objective

Make every current operational/install/runtime namespace explicitly identify this product as **CogentNexus-OpenClaw**, so a future **CogentNexus-HermesAgent** installation can coexist on the same machine without launcher, skill, plugin, state, task/service, environment, package, tool, log, backup, or uninstall/reset collisions.

This is an intentional breaking namespace migration before v1.0.0 acceptance.

Historical release/coordination evidence may preserve its original spelling when rewriting it would falsify prior evidence. Current executable surfaces, current documentation, current tests, packaging, and generated artifacts must use the isolated namespace.

## Canonical namespace contract

Use these canonical names:

| Surface | Canonical value |
|---|---|
| Product/display name | `CogentNexus-OpenClaw` |
| Short command namespace | `cnxclaw` |
| Windows launcher | `cnxclaw.cmd` |
| POSIX launcher | `cnxclaw` |
| Skill slug/directory | `cogentnexus-openclaw` |
| Skill frontmatter/display name | `CogentNexus-OpenClaw` |
| Workspace state root | `.cogentnexus-openclaw` |
| OpenClaw plugin ID | `cogentnexus-openclaw` |
| Plugin directory | `plugins/cogentnexus-openclaw` |
| npm package | `openclaw-plugin-cogentnexus-openclaw` |
| Plugin display name | `CogentNexus-OpenClaw Bridge` |
| Plugin root config key | `cogentNexusOpenClawRoot` |
| Environment prefix | `CNXCLAW_` |
| External tool prefix | `cnxclaw_` |
| Windows task/service prefix | `CogentNexus-OpenClaw-` |
| Windows application-data root | `CogentNexus-OpenClaw` |
| systemd basename | `cogentnexus-openclaw-supervisor` |
| launchd label | `ai.cogentnexus.openclaw.supervisor` |
| Release archive basename | `cogentnexus-openclaw-v<version>` |
| Installed policy template | `AGENTS.cogentnexus-openclaw.md` |
| Ownership product ID | `cogentnexus-openclaw` |

Use `cnxclaw.py` and `cnxclaw_v093.py` for the installed CLI modules instead of `cnx.py` and `cnx_v093.py`.

Rename current product-specific startup/stop/template filenames to include `cogentnexus-openclaw`.

Rename externally visible plugin tools from the generic `cogent_*` family to:

- `cnxclaw_rotation`
- `cnxclaw_workflow_start`
- `cnxclaw_ticket_status`
- `cnxclaw_knowledge`
- `cnxclaw_research`

New durable identifiers generated by this product must use a `CNXCLAW` namespace where a product prefix is required. Do not silently rewrite historical committed IDs.

## Scope inventory

The implementation must inspect and update all current namespace-bearing surfaces, including at least:

- `scripts/install.ps1`, `scripts/install.sh`, clean reinstall, repair, acceptance, release, and helper scripts;
- `skills/cogentnexus/**` renamed to `skills/cogentnexus-openclaw/**`;
- CLI/module/template filenames and all imports/references;
- `plugins/cogentnexus-rotation/**` renamed to `plugins/cogentnexus-openclaw/**`;
- plugin manifest ID/name/schema keys, package metadata/lockfile, tool registrations and tool references;
- state roots, databases, sockets/locks/markers, backups, staging, logs, diagnostics, and ownership metadata;
- Windows Scheduled Task XML/name, systemd, launchd, cron, Docker, Kubernetes, start/stop launchers;
- AGENTS policy templates and installer policy paths;
- CI workflows, package validation, release archive names and release titles;
- active README/install/clean-reinstall/provider/check/current-state documentation in English and Thai;
- tests, fixtures, expected strings, smoke checks, import loaders, and path filters;
- uninstall/reset/upgrade logic and all user-facing output.

Do not mechanically rewrite immutable historical coordination reports/reviews/tasks through Task 041 or already published release notes merely to change prose. Add an explicit historical-name note where current documentation links to legacy artifacts.

## Release version and release notes

This namespace change is part of **CogentNexus-OpenClaw v0.9.3**.

Required version work:

- update root `VERSION` from `0.9.2` to `0.9.3`;
- update the renamed OpenClaw Bridge package, manifest, and lockfile versions to `0.9.3` because plugin IDs, tool names, config keys, and package identity change;
- add `docs/releases/v0.9.3.md`;
- update current README/install/current-state/version references to the v0.9.3 CogentNexus-OpenClaw identity;
- make release archives `cogentnexus-openclaw-v0.9.3.zip` and `cogentnexus-openclaw-v0.9.3.tar.gz`;
- make release title `CogentNexus-OpenClaw v0.9.3`.

The v0.9.3 release note must explicitly explain that this release **กำหนดชื่อเรียกและ namespace ของแต่ละส่วนให้ชัดเจน**: every launcher, skill, plugin, state/config root, tool, task/service, package, artifact, reset, and uninstall boundary now identifies the OpenClaw product variant. The purpose is immediate human readability and guaranteed coexistence with the separately named CogentNexus-HermesAgent variant.

Classify this as an intentional namespace/API/installation-layout break, not a cosmetic text-only rename. Document the transactional legacy migration and removal of permanent generic aliases.

Do not create a tag, GitHub Release, or published archive in this task. Repository version metadata, release notes, packaging logic, and tests are in scope; actual release publication remains separately gated.

## Ownership manifest

Every new installation must create an ownership manifest under the new state root before MANAGED authority is claimed. It must contain at least:

- schema version;
- `productId: cogentnexus-openclaw`;
- display name;
- installed version;
- workspace identity/canonical path;
- skill path;
- plugin ID/path;
- launcher path;
- task/service identities;
- installation timestamp;
- migration source, if any.

Reset, uninstall, repair, and upgrade must verify this ownership manifest and fail closed on mismatch. They must never infer ownership merely because a path contains “cogentnexus”.

## Legacy migration contract

Legacy names are migration input only:

- `cnx.cmd` / `cnx`;
- `skills/cogentnexus`;
- `.cogent`;
- plugin `cogentnexus-rotation`;
- old CogentNexus supervisor/task/service names;
- old plugin config key and environment names.

The installer must support installation over an existing legacy CogentNexus-OpenClaw deployment without prior uninstall.

Required sequence:

1. inventory legacy and new namespace state without mutation;
2. prove the legacy installation belongs to this OpenClaw product using multiple identities (skill metadata, plugin ID, controller/state structure, expected launcher), not name alone;
3. reject ambiguous, mixed, corrupted, or foreign ownership before mutation;
4. if legacy mode is MANAGED or MAINTENANCE, invoke the old installed launcher exactly once to enter PASSTHROUGH/native OpenClaw before replacing files;
5. create a versioned migration backup under the new application-data namespace;
6. stage the new skill/plugin/state/launcher and rewrite absolute internal references transactionally;
7. validate the staged installation and ownership manifest;
8. install/enable the new plugin and new launcher;
9. remove old plugin registration/load path, legacy skill, launcher, task/service, and state only after the new installation is proven usable;
10. enter MANAGED/Ollama using only the new namespace;
11. verify no current legacy operational artifact remains;
12. on failure, fail safe in PASSTHROUGH and either roll back fully or leave an exact recoverable migration report. Never claim success from a mixed namespace.

After successful migration, do not leave a permanent `cnx.cmd`, `cnx`, generic skill, generic plugin, or generic state-root alias.

Legacy literals may remain only inside clearly named migration compatibility code/tests and an explicit scanner allowlist.

## Fresh-install contract

A fresh install must create only the new namespace.

It must not create or depend on:

- `cnx.cmd` or `cnx`;
- `skills/cogentnexus`;
- `.cogent`;
- `cogentnexus-rotation`;
- generic CogentNexus task/service/package/tool IDs.

All help, status, error, installation, reset, and uninstall text must say `CogentNexus-OpenClaw` and show `cnxclaw` commands.

## Reset and uninstall isolation

`cnxclaw reset` and `cnxclaw uninstall` retain explicit `y` confirmation.

They must use the ownership manifest and remove/reset only CogentNexus-OpenClaw-owned artifacts.

They must preserve:

- OpenClaw installation, configuration not owned by this product, sessions, and user data;
- Ollama installation, models, and user data;
- any CogentNexus-HermesAgent launcher, state, skill, plugin, task/service, package, logs, backups, and configuration;
- unrelated workspace files and plugins.

Uninstall removes the new launcher itself only after returning to native OpenClaw and verifying safe cleanup.

## Coexistence proof

Add deterministic tests that create sibling sentinels representing a future HermesAgent installation, for example:

- `cnxhermes.cmd`;
- `.cogentnexus-hermesagent`;
- `skills/cogentnexus-hermesagent`;
- plugin/config/task identifiers containing `cogentnexus-hermesagent`.

Record hashes before and after OpenClaw install-over, reset, and uninstall simulations. Every sibling sentinel must remain byte-identical and registered exactly as before.

Also prove that two products can have distinct state roots, launchers, skill IDs, plugin IDs, tool IDs, task/service names, log/backup roots, and ownership manifests in one workspace/machine model.

## Namespace lint

Create a repository validation tool such as `scripts/check_namespace_isolation.py`.

It must scan current operational code, templates, active documentation, workflows, tests, and package metadata for forbidden unqualified legacy product identifiers.

The scanner must:

- distinguish current code from immutable historical evidence;
- allow legacy literals only in explicit migration code/tests and a reviewed allowlist;
- fail on new unapproved `cnx.cmd`, `.cogent`, `skills/cogentnexus`, `cogentnexus-rotation`, generic plugin/package/task/service/tool IDs, or bare product-facing “CogentNexus” labels;
- run in CI and release packaging gates.

Do not make the allowlist broad enough to hide future collisions.

## Implementation safety

Use one isolated full clone of the freshly fetched coordination branch. Do not create/register another Git worktree and do not touch Task 027 or Task 038.

This task is repository-only. Do not install into the operator’s live OpenClaw workspace, change OpenClaw config, launch/stop/restart CogentNexus/OpenClaw/Ollama, run reset/uninstall, create/remove scheduled tasks/services, or mutate retained Procmon evidence.

Preserve the accepted runtime semantics except where namespace/path migration requires explicit changes.

Do not edit the v0.9.2 tag or rewrite Git history.

## Validation

At minimum run and report:

- the new namespace-isolation lint;
- baseline consistency validation;
- Python compile/validation/self-tests through new paths;
- full Python unit suite;
- plugin `npm ci`, build/validation, tests, evaluation, and production audit through the new directory;
- Windows installer/package smoke assertions;
- migration tests: clean legacy, PASSTHROUGH legacy, MANAGED legacy, ambiguous/mixed legacy, interrupted migration/rollback;
- fresh install layout assertions;
- install-over assertions;
- reset/uninstall isolation assertions;
- HermesAgent sibling-sentinel coexistence assertions;
- release archive/package naming assertions;
- old active-namespace absence assertions.

Do not perform live runtime or destructive Windows acceptance in this task.

## Acceptance gate

Return `PASS_OPENCLAW_NAMESPACE_ISOLATED` only if:

- all canonical names are implemented;
- fresh install creates only the new namespace;
- legacy install-over migration is transactional and proven;
- no permanent generic alias remains;
- reset/uninstall are ownership-manifest bounded;
- HermesAgent sentinels are untouched;
- current docs/help/packaging use the new names;
- namespace lint and all repository validation are green;
- the implementation commit contains no unrelated change.

Otherwise return an exact blocker such as:

- `BLOCKED_NAMESPACE_INVENTORY_INCOMPLETE`
- `BLOCKED_LEGACY_MIGRATION_UNSAFE`
- `BLOCKED_COEXISTENCE_ISOLATION`
- `BLOCKED_VALIDATION_FAILURE`
- `BLOCKED_PUBLICATION_UNSAFE`

## Publication

Implementation may modify all files required by the reviewed namespace migration and may perform intentional path renames. Do not include unrelated coordination/runtime evidence or retained Procmon artifacts.

Use a commit message beginning:

`feat: isolate CogentNexus-OpenClaw namespace`

Publish the matching report only at:

`docs/operations/coordination/reports/CNX-20260824-042-openclaw-namespace-isolation.md`

Report exact changed paths, rename map, legacy-literal allowlist, validation commands/results, commit SHA, side-effect accounting, and remaining uncertainty.

## Progress communication

Report meaningful progress approximately every 3 minutes and immediately after inventory, canonical rename map, migration implementation, install/reset/uninstall isolation, coexistence tests, namespace lint, full validation, and publication/blocker.

Progress reports are not pause points.
