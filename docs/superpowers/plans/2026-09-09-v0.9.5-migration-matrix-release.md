# v0.9.5 Migration, Behavior Matrix, and Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Converge the provider/runtime, delivery/session, and Supervisor/idle repairs into one exact v0.9.5 candidate, prove upgrade compatibility and cross-subsystem behavior, then publish only after CI, Windows lifecycle, idle-quiescence, and live provider-switch acceptance pass.

**Architecture:** Provider/runtime repair establishes authority boundaries first. Delivery/session identity builds on that stable boundary. Supervisor single-wake then consumes canonical delivery/provider/session authority. Migration/matrix/release runs last and is not allowed to compensate for unfinished subsystem behavior.

**Tech Stack:** Python, TypeScript/Vitest, SQLite, GitHub Actions, PowerShell/Windows installer lifecycle, GitHub Release workflow, checksum/provenance scripts.

## Execution status

Tasks 1–3 of this plan are implemented and verified on PR #33. No version, tag, or release publication was performed.

### Task 1 — v0.9.4 -> v0.9.5 migration safety

- [x] v0.9.4 Host-state matrix for managed / maintenance / passthrough + plugin enabled/disabled.
- [x] Provider metadata is excluded from canonical Host authority state.
- [x] Cloud pass-through fixture proves the OpenClaw-owned route/auth/model configuration snapshot is unchanged by migration translation.
- [x] Durable Ticket/session/delivery rows are asserted to remain present during the migration fixture.
- [x] Legacy state loading is read-only.

### Task 2 — Provider-independent behavior matrix

- [x] Python matrix covers local Ollama, Cloud, and unknown-future providers against normal, gateway-restart, provider-switch, and idle events.
- [x] Disabled CNX remains the native OpenClaw boundary.
- [x] Idle runtime produces no wake when durable work is empty.
- [x] Final Direct-result delivery takes precedence over an already-completed Ticket.
- [x] Plugin matrix covers lifecycle-generation neutrality and exact delivery fencing across Web Chat / Discord semantics.

### Task 3 — Validation gate

- [x] Full repository Validate workflow green on exact candidate `66fb3dd8e8a74307f0cfb24eb12e64784896b6d1`.
- [x] Windows Installer Pack Smoke green.
- [x] PS5.1 Acceptance Smoke green.
- [x] PS5.1 Live Runner Smoke green.
- [x] No mandatory workflow modification was required to discover the new tests.

### Release gate

- [ ] Remaining release/migration tasks, including version bump, release-facing documentation, install-over acceptance, behavior-matrix expansion where still required, and final publication evidence, remain intentionally unexecuted.
- [ ] No v0.9.5 tag/release has been created by this work.
