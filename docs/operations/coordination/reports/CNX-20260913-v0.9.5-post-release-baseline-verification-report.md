# v0.9.5 Post-release Baseline Verification — Final

## Decision

**BASELINE OPERATIONALLY CLOSED — published release untouched**

The v0.9.5 release tag, remote `main`, installed plugin bytes, loaded runtime, Gateway, Ollama readiness, and zero-outbox state are now aligned and verified. A legacy system-check assertion still reports a contradictory provider-selection failure; it is recorded separately and is not treated as a runtime failure because the authoritative runtime status and provider checks are healthy.

## Exact release identity

- Repository: `funggier/CogentNexus-OpenClaw`
- `main`: `50be0b973c30fd8d1528aaac3497c0fc3b0b4d95`
- `v0.9.5`: `50be0b973c30fd8d1528aaac3497c0fc3b0b4d95`
- GitHub Release: `v0.9.5`, published, not draft
- Release URL: https://github.com/funggier/CogentNexus-OpenClaw/releases/tag/v0.9.5
- Tag/release target: exact merge SHA above

## Exact-tag installation verification

A fresh detached clone was created from `v0.9.5` at:

`C:\Users\CDQ-P\AppData\Local\Temp\CogentNexus-OpenClaw-v0.9.5-baseline`

Verified:

- clone HEAD: `50be0b973c30fd8d1528aaac3497c0fc3b0b4d95`
- clone worktree: clean
- repository `VERSION`: `0.9.5`
- plugin package version: `0.9.5`
- installer executed successfully from this exact detached tag checkout
- installer reported: `CogentNexus-OpenClaw v0.9.5 installation completed successfully.`

The installed plugin is loaded by OpenClaw:

- status: `loaded`
- version: `0.9.5`
- install path: `C:\Users\CDQ-P\.openclaw\extensions\cogentnexus-openclaw`
- recorded version: `0.9.5`
- entry file SHA-256: `da8e810e88547ef866e2279fde86195f02557a9ca0a35a8771a0e646a1f6a7ef`
- exact `dist` tree: 240 files, tree fingerprint `34249902f70606a5238868d33197d3b8992aed1cf45e9b06153e2490559381bd`

The exact-tag source `dist` tree and installed plugin `dist` tree produced the same file count and fingerprint.

## Runtime baseline

Read-back from `cnxclaw.cmd status`:

- mode: `managed`
- CNX mode: `active`
- Gateway: healthy
- Ollama: installed, reachable, healthy, ready
- Ollama models available: 3
- pending outbox: `0`
- OpenClaw plugin: loaded, version `0.9.5`

The selected OpenClaw model was set to `ollama/qwen3.6:27b` and read back successfully. This changed local runtime configuration only; it did not modify source, the tag, `main`, or the published Release.

Read-only checks also passed for:

- provider discovery: Ollama
- Gateway connectivity
- Ticket store integrity
- maintenance/recovery fence
- supervisor health
- delivery/outbox
- disk and memory headroom

## Known checker anomaly

`cnxclaw.cmd check system` still exits `2` and reports:

`Provider selection — Multiple providers are installed but no provider has been selected`

At the same time, the same check reports the OpenClaw default model as `ollama/qwen3.6:27b`, and `cnxclaw.cmd status` reports Ollama `ready: true` and Gateway healthy. This is a contradictory legacy checker assertion, not evidence that the installed v0.9.5 runtime is inactive or that the Ollama route is unavailable. It was not fixed post-release because doing so would modify source after publication.

LM Studio is installed but unreachable and remains **not required** for the v0.9.5 release gate.

## GitHub Actions evidence

For exact SHA `50be0b973c30fd8d1528aaac3497c0fc3b0b4d95`:

- 19 completed check-runs were observed.
- All had `status: completed`, `conclusion: success`, and the exact same `head_sha`.
- The legacy commit-status collection had zero entries; it was not interpreted as CI pass/fail.

## Scope protection

- Published Release was not edited.
- `v0.9.5` was not moved.
- `main` was not modified.
- No commit or source-code change was created.
- No unrelated Release asset was uploaded.
- The original stale/dirty clone was not cleaned or changed.

## Final conclusion

The post-release baseline is **operationally closed** with durable evidence that release identity and installed/active v0.9.5 runtime are aligned. The remaining checker anomaly is explicitly quarantined as a post-release diagnostic discrepancy and must not be silently promoted to PASS or used to claim a product failure.
