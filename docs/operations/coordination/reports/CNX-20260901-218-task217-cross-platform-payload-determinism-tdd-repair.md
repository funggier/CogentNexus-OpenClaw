# CNX-20260901-218 — Task-217 Cross-Platform Payload Determinism TDD Repair

Date: 2026-09-01 ICT  
Parent: `CNX-20260901-217`  
Execution mode: `TASK218_CROSS_PLATFORM_PAYLOAD_DETERMINISM_TDD_REPAIR`  
Executor: Hermes / repository engineer  
Coordinator / final reviewer: ChatGPT

## Final disposition

`FAIL_RED_NOT_REPRODUCED`

Task 218 is **not accepted as PASS**. The exact candidate passed repository CI and produced matching CI/Windows fingerprints, but the required platform-independent genuine RED regression was not proven. No installer requalification or Discord traffic is authorized by this report.

## Authority and candidate

Fresh remote authority was read from `origin/agent/v0.9.3-full-stabilization` before reporting:

```text
REMOTE_HEAD=e2dede9a0cb16b8b9536a350e018bfbd7c95c39b
Task ID=CNX-20260901-218
Status=READY_FOR_HERMES
```

Published `v0.9.3` remains immutable at `26ce64a624255278a3a0266ad38746e0e6ed2e31`.

The candidate under examination was:

```text
e2dede9a0cb16b8b9536a350e018bfbd7c95c39b
```

## Repository and CI evidence

The following local checks passed before this report:

```text
focused payload determinism test: PASS (the then-current helper fixture)
npm run build: PASS
npm test: 56 files / 280 tests passed
npm run evaluation: PASS (passed=true)
npm run plugin:validate: PASS
packedFileCount: 192
```

Exact authoritative CI runs for `e2dede9a0cb16b8b9536a350e018bfbd7c95c39b`:

| Workflow | Run ID | Result |
|---|---:|---|
| Validate | `33521283353` | success |
| PS5.1 Acceptance Smoke | `33521283398` | success |
| Windows Installer Pack Smoke | `33521283517` | success |

The Validate package-proof artifact was:

```text
artifact id: 9805795685
artifact name: cogentnexus-openclaw-v0.9.3-package-proof-e2dede9a0cb16b8b9536a350e018bfbd7c95c39b
packageVersion: 0.9.3
payloadFileCount: 192
payloadV2Fingerprint: 18a9003b47347bd598e58bef54f453313df8032943f5436cb9ed9096fe4bea14
archiveSha256(tar.gz): 5abe9c7d0b8143aedf56c9f58367f3243ffe09938d62fd6ea1f63ab7d02a746f
archiveSha256(zip): 21c25e57d98974964f7ff7b58835fe39864345a7e12142c8abb81fc8da1bf75a
```

## Windows reproduction

A fresh clean Windows checkout was created at the exact candidate SHA. `npm ci`, `npm run build`, and `npm run plugin:validate` completed successfully. Repository-supported fingerprint output was:

```text
Windows payload fingerprint:
18a9003b47347bd598e58bef54f453313df8032943f5436cb9ed9096fe4bea14

CI payload fingerprint:
18a9003b47347bd598e58bef54f453313df8032943f5436cb9ed9096fe4bea14

payload path count: 192
fingerprint equality: true
```

The generated installable payload contained no CRLF in the fresh Windows check.

This proves CI/Windows identity equality for this candidate, but it does not repair the missing RED proof described below.

## TDD RED adjudication

The first test-only commit was:

```text
96a8a1b59a434c0ed87fd74b32471db26ff2bb31
```

A later test refinement commit was:

```text
385de3cbb4bd59aaf4a0e6ded98977fffe673ddd
```

The initial attempts that failed did so because of test-harness defects, including invalid module syntax, incorrect fixture paths, dependency isolation issues, and a missing symbol. Those are not genuine newline-variance RED failures.

A subsequent real-TypeScript LF/CRLF fixture probe was run without canonicalization and returned:

```text
redEqual=true
```

The same probe with source maps enabled also returned:

```text
redEqual=true
```

Therefore the required assertion failure due to differing generated bytes/fingerprints was not established. The test does not currently satisfy the Task-218 requirement to create two isolated LF/CRLF source fixtures and prove that the pre-fix normal build/package path fails.

## Implementation status

The candidate implementation included:

- a post-build `dist` LF canonicalization utility;
- invocation from the normal `build` script;
- deterministic sorted traversal and symlink rejection in the utility.

However, because the genuine RED regression was not proven, the implementation is not accepted as a completed Task-218 repair. The fingerprint algorithm was not intentionally modified, and no installer or live runtime operation was performed.

## Live preservation and mutation ledger

No live installer, lifecycle, Gateway, ownership, transaction, SQLite, provider/model, Discord, release, tag, or asset operation was performed for Task 218.

```text
installer/install-over: 0
cnxclaw lifecycle actions: 0
live plugin/config mutation: 0
Gateway restart: 0
live SQLite writes: 0
provider/model substitution: 0
Discord Sends: 0
Release/tag/asset mutation: 0
force push: 0
```

The local authority checkout was restored to a clean worktree before this report was written:

```text
WORKTREE_CLEAN=true
```

Previously established live baseline remains the preserved state: controller `passthrough`, generation `33`, old live fingerprint `f82674172a3946e00ddcb3a94fd14c8476bf91abc11ed7d44b5fa53acb74eaf1`, Gateway healthy, Delivery `READY`, Recovery `READY`, and Task-205 recovery cancelled/inert. No Task-218 action was performed against that runtime.

## Decision

`FAIL_RED_NOT_REPRODUCED`

Do not proceed to installer requalification or Discord acceptance from this report. A follow-up repair must first produce a genuine platform-independent RED test using the real build/package boundary, then a minimal GREEN implementation, new exact CI proof, and fresh Windows byte comparison.
