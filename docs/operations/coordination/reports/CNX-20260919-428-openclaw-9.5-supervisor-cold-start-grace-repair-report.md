# CNX-20260919-428 — OpenClaw 9.5 Supervisor Cold-Start Grace Repair Report

## Final classification

`OPENCLAW_95_SUPERVISOR_STARTUP_GRACE_REPAIR_GREEN`

CNX-428 repaired a live restart-loop interaction between the CogentNexus external supervisor and the slower OpenClaw 2026.9.5 startup lifecycle.

The parent CNX-427 final genuine Discord acceptance remains pending.

## Authority

- Repository: `funggier/CogentNexus-OpenClaw`
- Branch: `cnx-357-openai-dashboard-ticket-first-requalification-v2`
- Parent task: `CNX-20260919-427`
- Pre-repair checkpoint: `4143ac139e347eaf12a6116463faa267be5475fb`
- Repair implementation commit: `13dfba9a55f4d64ceb9aa8440670c9ee9792354a`
- Live OpenClaw: `2026.9.5 (ec9c1a1)`

## Live root-cause evidence

The restart was not a Node crash and was not caused by a persistent modification to OpenClaw dist.

Process inspection proved the external actor was the CogentNexus supervisor chain, including commands equivalent to:

`runtime.py lifecycle restart --reason "CogentNexus-OpenClaw external supervisor confirmed an unresponsive Gateway"`

OpenClaw itself logged an accepted `gateway.restart.request` followed by `SIGUSR1: gateway.restart`.

The key timing evidence from a controlled service-owned cold start was:

- startup: approximately `19:43:31 +07`;
- HTTP listener: `19:44:13.586 +07`, about `42.4s`;
- liveness warning during `sidecars.model-runtime`: event-loop delay around `38.3s`;
- startup outcomes: `19:45:01.617 +07`;
- `gateway ready`: `19:45:01.621 +07`.

Total cold-start time was approximately `90.7s`.

The previous supervisor contract used two lightweight HTTP probes separated by only `1.0s`. On OpenClaw 9.5, this was insufficient evidence of a hard hang.

## Production repair

Changed:

- `skills/cogentnexus-openclaw/scripts/host_v091.py`
- `tests/test_host_v091.py`

The repair adds:

`GATEWAY_STARTUP_GRACE_SECONDS = 180.0`

and `gateway_startup_grace()`.

The function reads the active OpenClaw state directory, opens:

`state/openclaw.sqlite`

read-only, and selects the newest incomplete row from:

`gateway_boot_lifecycle`.

If the row is no older than 180 seconds, a confirmed double fast-probe failure returns a bounded observe-only result:

- `result=gateway-starting`;
- `action=none`;
- `wakeReason=gateway/startup-grace`;
- `gatewayHealthy=false`;
- `providerRequired=false`;
- `heavyPath=false`.

No provider/model routing authority was added.

If no active boot is available, the read fails, or the row has exceeded 180 seconds, the existing hard-hang recovery remains in force.

## TDD / regression evidence

Focused `tests.test_host_v091`:

`8/8 PASS`

Expanded Python regression set:

- `tests.test_host_v091`
- `tests.test_host_v091_actionability`
- `tests.test_v091_idle_recovery_hint`
- `tests.test_host_provider_v092`

Result:

`44/44 PASS`

Additional gates:

- `python -m py_compile` — PASS
- `git diff --check` — PASS

Canonical plugin full suite:

- test files: `84 passed / 1 failed` out of `85`;
- tests: `381 passed / 1 failed` out of `382`.

The sole failure is the pre-existing intentional RED:

`src/cnx383-hook-policy-projection.test.ts`

whose own source explicitly states it intentionally fails until the older CNX-383 normalized-hook-policy projection boundary is repaired.

No CNX-428 production or test change touches that TypeScript boundary.

Relevant current tests remained GREEN, including:

- `cnx427-external-ingress-runid.test.ts`: `5/5 PASS`;
- `cnx422-reply-dispatch-admission.test.ts`: `16/16 PASS`;
- `index.test.ts`: `42/42 PASS`;
- Discord delivery/inference suites;
- session generation/wiring;
- direct model-call lease and recovery fences.

## Live deployment evidence

The reviewed `host_v091.py` was copied to the installed plugin.

Source/live SHA256:

`239D3818CDCD25F33CBAA348BEF504741D1117A063D433EA87E207104833621E`

Pre-repair installed backup:

`T:\CogentNexus\CogentNexus-OpenClaw\diagnostics\CNX-20260919-427\host_v091.pre-startup-grace.py`

Backup SHA256:

`99906331AB7E406EA6C6B4B2F493902A904DFF8A50E8D79272FCBACA8A698D23`

No backup was placed on C:.

### Actual boot-grace proof

During the real service-owned OpenClaw boot:

- boot id: `2457011a-c738-4e74-912d-3f309236455d`;
- observed age: approximately `35.38s`;
- grace: `180s`;
- classification: `active-boot-grace`.

A live-process qualification forced the lightweight Gateway probe to false and replaced `_restart_unresponsive_gateway` with a throwing sentinel.

Observed result:

- `result=gateway-starting`;
- `action=none`;
- `wakeReason=gateway/startup-grace`;
- `heavyPath=false`.

The throwing restart sentinel was never invoked.

### Service ownership / maintenance convergence

Gateway ownership was restored to the registered Windows Scheduled Task.

Observed service child:

- PID `30416`;
- command included `--task-supervisor-child=145790647`;
- port `18789` owned by the same service child.

A stale `healthy-runtime` marker created by the pre-repair restart loop was then reconciled through the supported supervisor/lifecycle path.

Result:

- supervisor one-shot: `idle`;
- `gatewayHealthy=true`;
- `heavyPath=false`;
- `maintenanceRecovery.status=reconciled`;
- marker removed;
- no restart worker remained.

Recurring supervisor was re-enabled.

Natural cadence evidence:

- `CogentNexus-OpenClaw-Supervisor`: Enabled;
- `LastTaskResult=0`;
- Gateway health: `ok=true`;
- Discord: `ready/connected`;
- no new post-repair `gateway.restart.request` observed.

## CNX-427 isolated Ticket-first qualification

A first long-running `powershell_run` probe was contaminated by connector-level long-call behavior and materialized four separate OpenClaw user turns with four distinct idempotency keys. It is explicitly excluded from one-turn acceptance evidence.

A second probe used LConnect `start_process` so one operating-system process/PID represented one CLI invocation:

- process PID: `10260`;
- process session: `proc-1789822764718-54`;
- session key: `agent:main:cnx427-process-probe-1958`;
- model: `ollama/qwen3:1.7b`;
- delivery: disabled.

Observed authoritative identity:

- run: `62a61c83-8c61-446c-9a6d-20b4138dd857`;
- Ticket: `CNXT-7a349016-6de3-4f20-bc7a-73c82cca6603`;
- transcript user turns matching the probe: exactly one;
- CNX Tickets for the probe session: exactly one;
- CNX model calls: exactly one.

Ordering:

1. Ticket `accepted` — `12:59:26.475Z`;
2. Ticket `routed` — `12:59:26.478Z`;
3. `direct_model_call_started` — `12:59:26.521Z`;
4. `inference_attempt_started` — `12:59:26.532Z`.

Therefore Ticket persistence preceded model-call authority by approximately 46 ms.

The small local model did not complete inside the explicit 90-second CLI timeout:

- model-call duration: `89943ms`;
- terminal outcome: `error / timeout`;
- Ticket status: `failed`;
- failure class: `permanent`;
- no recovery inference;
- no duplicate Ticket;
- no duplicate model call.

This isolated qualification proves the OpenClaw 9.5 Gateway generation now carries the CNX Ticket-first admission hook into the authoritative execution boundary. It does not replace the parent task's final genuine Discord delivery acceptance.

The small model was unloaded after the probe with `ollama stop qwen3:1.7b`.

## Remaining parent gate

CNX-427 remains `IN_PROGRESS`.

The remaining acceptance requires one genuine operator-originated Discord turn on OpenClaw 2026.9.5 proving, for that single external ingress:

- one authoritative host run;
- exactly one CNX Ticket;
- Ticket persisted before inference/model-call authority;
- one model execution;
- one Discord delivery;
- correct terminal Ticket state;
- no duplicate or stale lane.

No further synthetic semantic probe is required before that operator turn.
