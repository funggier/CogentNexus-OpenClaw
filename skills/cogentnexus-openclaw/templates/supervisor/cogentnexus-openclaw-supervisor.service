[Unit]
Description=CogentNexus-OpenClaw deterministic runtime supervisor
After=default.target

[Service]
Type=oneshot
ExecStart={{PYTHON}} {{RUNTIME}} --root {{ROOT}} supervisor tick --execute-safe
TimeoutStartSec=90
StandardInput=null
StandardOutput=journal
StandardError=journal
