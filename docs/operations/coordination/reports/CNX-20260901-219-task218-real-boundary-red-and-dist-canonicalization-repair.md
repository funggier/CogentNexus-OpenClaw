# CNX-20260901-219 — Task-218 Real-Boundary RED and Dist Canonicalization Repair

Date: 2026-09-01 ICT  
Parent: `CNX-20260901-218`  
Execution mode: `TASK219_REAL_BOUNDARY_RED_AND_DIST_CANONICALIZATION_REPAIR`  
Executor: Hermes / repository engineer  
Coordinator / final reviewer: ChatGPT

## Final disposition

`FAIL_CROSS_PLATFORM_DETERMINISM`

Task 219 produced a genuine real-boundary RED and a bounded generated-`dist` canonicalizer, but the final fresh Windows package identity did not match authoritative CI. The repair is therefore not accepted as PASS and is not installer-authorized.

## Authority and commits

Fresh remote authority was read before execution:

```text
Task: CNX-20260901-219
Status: READY_FOR_HERMES
Remote authority before execution: c4ecf926a74967f0a89ed7307e1af1ce9aafe06d
```

Forward-revert of the unaccepted Task-218 production effects:

```text
3361ddb0e9de48a4caaf43df96680e4e969f0273
```

Corrected genuine RED test commit:

```text
107c957e1b1ca95a369be9c346f97ef4d6e8c5eb
```

Bounded GREEN dist canonicalizer commit:

```text
9af329b4de7c02fda35b467d84e76bb0f0bb0944
```

Targeted static payload checkout defense-in-depth commits:

```text
4e31dbd79cd4c0a7eb161888c14221f0ae03bcc0
b081d55c4ffa5fcb03931dc320d39bdcf92a6cf5
```

The final candidate tested against CI was:

```text
b081d55c4ffa5fcb03931dc320d39bdcf92a6cf5
```

## Genuine RED evidence

The corrected regression used the real plugin source/build boundary. It created isolated LF-source and CRLF-source copies, installed dependencies independently, ran the normal pre-fix `npm run build`, and compared the complete generated `dist` tree.

The effective pre-fix result was a genuine assertion failure:

```text
LF dist files: 188
CRLF dist files: 188
differing generated files: 43
```

The first differing paths included:

```text
external-research.js
knowledge-store.js
ticket-runtime.test.js
ticket-store.js
v084-entry.js
v084.js
v090-compaction-boundary.js
v090-compaction-boundary.test.js
v090-compaction-observer.test.js
v090-context-guard.js
```

The failure was an assertion about generated byte differences, not a syntax, import, dependency, path, or harness error.

## GREEN implementation

The canonicalizer is bounded to the generated `dist` root and:

- traverses entries in deterministic sorted order;
- uses `Dirent` and `lstat` checks before descend/read/write;
- rejects symbolic-link indirection;
- rejects unsupported filesystem entries and generated artifact extensions;
- converts CRLF to LF while preserving other bytes;
- is idempotent;
- is invoked automatically after TypeScript emit by the normal `build` script;
- does not modify the fingerprint algorithm;
- does not normalize tracked source files during build.

Local GREEN checks passed:

```text
corrected real-boundary regression: PASS
npm run build: PASS
npm test: 56 files / 280 tests passed
npm run evaluation: PASS (passed=true)
npm run plugin:validate: PASS
packedFileCount: 192
canonicalizer second pass: canonicalized 0 dist text files to LF
```

## Authoritative CI

All required workflows passed for the intermediate exact candidate `9af329b4de7c02fda35b467d84e76bb0f0bb0944`:

| Workflow | Run ID | Result |
|---|---:|---|
| Validate | `33525364442` | success |
| Windows Installer Pack Smoke | `33525364411` | success |
| PS5.1 Acceptance Smoke | `33525364408` | success |

After the targeted static-byte changes, a new candidate `b081d55c4ffa5fcb03931dc320d39bdcf92a6cf5` was pushed for the next CI cycle. This report does not claim a complete CI gate for that later SHA because the independent Windows identity gate failed before a valid PASS disposition could be established.

## Package proof and identity failure

The authoritative package proof for the prior GREEN candidate contained:

```text
artifact id: 9807757662
sourceCommit: 4e31dbd79cd4c0a7eb161888c14221f0ae03bcc0
packageVersion: 0.9.3
payloadFileCount: 192
payloadV2Fingerprint: e3bcce04c3af57a7c0dd596203464e197c80e9d2c903593f73e032caa96f9386
tar.gz SHA-256: 0d3dfa5d7fee340224fef298cd723f2427adca7e30490c4a2b94ae121444bd77
zip SHA-256: 035c6dbcd16688f0f38742fda716347b72d29d53ad317ead07accbcdbaade0d5
```

Fresh Windows checkout of `4e31dbd…` passed normal preparation and generated-dist checks, but repository-supported identity was:

```text
Windows fingerprint: 4ed5231ce6c2718fad2fed0dcf6cbe104ae856ecc60513904c7e45f29725f9f4
CI fingerprint:      e3bcce04c3af57a7c0dd596203464e197c80e9d2c903593f73e032caa96f9386
```

The corrected file-by-file comparison showed:

```text
dist differences: 0
static differences: 3
```

The remaining files were:

```text
README.md
openclaw.plugin.json
scripts/bootstrap-ticket-db.mjs
```

Each remaining difference was CRLF in the Windows checkout versus LF in the CI archive. The Windows checkout also became tracked-worktree dirty for those three files despite the targeted checkout attributes. This fails the exact Windows identity and cleanliness gates.

The subsequent `-text` attribute experiment did not prove a clean fresh checkout; `README.md`, the manifest, and bootstrap script still checked out with CRLF and the worktree was dirty. No further claim is made from that experiment.

## Indirection boundary

The canonicalizer implementation contains no-follow checks and rejects `Dirent.isSymbolicLink()` entries. A valid Windows junction fixture could not be created in this environment: the native `mklink /J` harness returned a path-syntax error. Therefore junction/reparse-point rejection remains unproven by an executable fixture and is not counted as PASS evidence.

## Live preservation and mutation ledger

No live product operation was performed:

```text
installer/install-over: 0
cnxclaw lifecycle actions: 0
live plugin/config mutation: 0
Gateway restart: 0
live ownership/staging/transaction/SQLite writes: 0
provider/model substitution: 0
Discord Sends: 0
Release/tag/asset mutation: 0
force push/history rewrite: 0
```

All builds and package comparisons were isolated. The previously established live baseline was preserved: controller `passthrough`, generation `33`, old live plugin fingerprint retained, Gateway healthy, Delivery `READY`, Recovery `READY`, and Task-205 recovery cancelled/inert.

## Decision

`FAIL_CROSS_PLATFORM_DETERMINISM`

Do not proceed to installer requalification or Discord acceptance. The generated `dist` variance repair is locally proven, but the final package identity remains non-deterministic on this Windows checkout because three static payload files still differ at the checkout boundary. A later task must establish a verified static-byte strategy and rerun exact CI plus fresh Windows comparison before installation is authorized.
