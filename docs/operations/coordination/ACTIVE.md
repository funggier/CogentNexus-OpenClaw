# Active Coordination Task

Status: `READY_FOR_CODEX`  
Task ID: `CNX-20260822-001`  
Updated: 2026-08-22 20:36 ICT  
Owner: ChatGPT  
Executor: Codex  

## Active task

[`tasks/CNX-20260822-001-gateway-convergence.md`](tasks/CNX-20260822-001-gateway-convergence.md)

## Purpose

Run the dedicated Gateway durable-recovery convergence diagnostic on the real Windows machine and determine whether the recovery/maintenance boundary clears itself after the Gateway is automatically restored, without an operator recovery command forcing the state transition.

## Codex standing instruction

Codex should already have accepted [`CODEX_BOOTSTRAP.md`](CODEX_BOOTSTRAP.md).

On the operator signal:

```text
ต่อ
```

Codex must:

1. synchronize with the coordination branch safely;
2. re-read [`README.md`](README.md), [`SIGNALS.md`](SIGNALS.md), this file, the active task, and [`reports/README.md`](reports/README.md);
3. verify task preconditions;
4. execute the active task exactly;
5. push the matching report/evidence references;
6. stop and wait for ChatGPT review.

Do not edit this file to report progress. The execution result belongs in the task's matching file under `reports/`.

## Duplicate-execution fence

If a completed report for this Task ID already exists at the synchronized GitHub state, do not repeat disruptive effects merely because `ต่อ` was sent again. Report that the task is awaiting review or already completed and stop.

## Completion rule

This pointer remains `READY_FOR_CODEX` until Codex publishes the matching report. ChatGPT will then review that report and advance this pointer to the next authorized task, rework, blocked state, or closure.
