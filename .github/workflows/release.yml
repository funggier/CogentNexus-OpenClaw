name: Release

on:
  push:
    tags:
      - "v*"
    branches:
      - "release/v*"

permissions:
  contents: write

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: plugins/cogentnexus-rotation/package-lock.json
      - run: python -m pip install -r requirements-dev.txt
      - run: python scripts/check_baseline_consistency.py
      - run: python skills/cogentnexus/scripts/validate.py --workspace-singleton
      - run: python skills/cogentnexus/scripts/cogent.py self-test
      - run: python skills/cogentnexus/scripts/runtime.py self-test
      - run: python skills/cogentnexus/scripts/workflow.py self-test
      - run: python -m py_compile skills/cogentnexus/scripts/host.py skills/cogentnexus/scripts/host_v091.py skills/cogentnexus/scripts/host_control.py skills/cogentnexus/scripts/host_control_v091.py skills/cogentnexus/scripts/lifecycle_v091.py skills/cogentnexus/scripts/startup.py skills/cogentnexus/scripts/startup_v091.py
      - run: python -m unittest discover -s tests -v
      - name: Test plugin path migration helper
        shell: bash
        run: |
          printf '["/tmp/cogentnexus-rotation","/tmp/keep"]' | \
            python scripts/filter_plugin_paths.py --plugin-id cogentnexus-rotation | \
            python -c 'import json,sys; assert json.load(sys.stdin) == ["/tmp/keep"]'
      - run: npm ci
        working-directory: plugins/cogentnexus-rotation
      - run: npm test
        working-directory: plugins/cogentnexus-rotation
      - run: npm run evaluation
        working-directory: plugins/cogentnexus-rotation
      - run: npm audit --omit=dev
        working-directory: plugins/cogentnexus-rotation
      - run: npm run plugin:validate
        working-directory: plugins/cogentnexus-rotation
      - name: Resolve release tag
        id: release
        shell: bash
        run: |
          set -euo pipefail
          if [[ "$GITHUB_REF_TYPE" == "tag" ]]; then
            tag="$GITHUB_REF_NAME"
          else
            tag="${GITHUB_REF_NAME#release/}"
          fi
          [[ "$tag" == v* ]]
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
      - name: Verify release version and baseline artifacts
        shell: bash
        run: |
          set -euo pipefail
          expected="${{ steps.release.outputs.tag }}"
          expected="${expected#v}"
          version_file=$(tr -d '\r\n' < VERSION)
          package_version=$(node -p "require('./plugins/cogentnexus-rotation/package.json').version")
          manifest_version=$(node -p "JSON.parse(require('fs').readFileSync('./plugins/cogentnexus-rotation/openclaw.plugin.json')).version")
          lock_version=$(node -p "require('./plugins/cogentnexus-rotation/package-lock.json').version")
          lock_root_version=$(node -p "require('./plugins/cogentnexus-rotation/package-lock.json').packages[''].version")
          test "$expected" = "$version_file"
          test "$expected" = "$package_version"
          test "$expected" = "$manifest_version"
          test "$expected" = "$lock_version"
          test "$expected" = "$lock_root_version"
          test -s docs/BASELINE.md
          test -s docs/INSTALL.md
          test -s docs/INSTALL.th.md
          test -s "docs/releases/v${expected}.md"
      - name: Build release archives
        shell: bash
        run: |
          set -euo pipefail
          tag="${{ steps.release.outputs.tag }}"
          name="cogentnexus-$tag"
          stage="$RUNNER_TEMP/$name"
          out="$RUNNER_TEMP/release"
          mkdir -p "$stage" "$out"
          rsync -a \
            --exclude '.git/' \
            --exclude '.cogent/' \
            --exclude 'node_modules/' \
            ./ "$stage/"
          tar -C "$RUNNER_TEMP" -czf "$out/$name.tar.gz" "$name"
          (cd "$RUNNER_TEMP" && zip -qr "$out/$name.zip" "$name")
          (cd "$out" && sha256sum "$name.tar.gz" "$name.zip" > SHA256SUMS.txt)
      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          tag="${{ steps.release.outputs.tag }}"
          name="cogentnexus-$tag"
          notes="docs/releases/$tag.md"
          if gh release view "$tag" >/dev/null 2>&1; then
            echo "Release $tag already exists; refusing duplicate publication."
            exit 1
          fi
          common=(
            "$RUNNER_TEMP/release/$name.tar.gz"
            "$RUNNER_TEMP/release/$name.zip"
            "$RUNNER_TEMP/release/SHA256SUMS.txt"
          )
          if [[ "$GITHUB_REF_TYPE" == "tag" ]]; then
            gh release create "$tag" "${common[@]}" --verify-tag --notes-file "$notes" --title "CogentNexus $tag"
          else
            gh release create "$tag" "${common[@]}" --target "$GITHUB_SHA" --notes-file "$notes" --title "CogentNexus $tag"
          fi
