[Unit]
Description=CogentNexus deterministic runtime supervisor
After=default.target

[Service]
Type=oneshot
ExecStart={{PYTHON}} {{PHASE3}} --root {{ROOT}} supervisor tick --execute-safe
TimeoutStartSec=90
